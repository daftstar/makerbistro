<h1>Bienvenue Le Grand Fromage!</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @menu_items.each do |menu_item| %>
      <tr>
        <td><p><%= menu_item.name %></p>
        <% menu_item.ingredients.each do |ingredientName|%>
          <em><%= ingredientName.name%>,</em>
        <% end %>




        </td>

        <td><p>$<%= menu_item.price %></p></td>




        <td><%= link_to 'Show', menu_item %></td>
        <td><%= link_to 'Edit', edit_menu_item_path(menu_item) %></td>
        <td><%= link_to 'Destroy', menu_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>

    <% end %>
  </tbody>
</table>
<hr>
<br>

<%= link_to 'New Menu item', new_menu_item_path %>
